# Surprise ledger — выводы, противоречащие прежним планам

> Пустой ledger = подозрение на шаблонность. Каждая запись: что ожидалось (где записано) → что
> оказалось → основание → следствие для плана.

| # | Ожидалось (источник) | Оказалось | Основание | Следствие |
|---|---|---|---|---|
| S-1 | BS-index «измеряет надёжность» с 5 взвешенными сигналами (`graph.md` формула, `ozalupennieStrategic5` §BS-v2 «добавить специфичность») | В проде 4 из 5 членов ≡ 0; `bs_index ∈ [0,20]`; калибровка ранговая → 10% «critical» при любых данных | `40-data-surface.md` D1/D6; C-04 | Не чинить, заморозить; специфичность (BS-v2) — не спасла бы мёртвую формулу |
| S-2 | «Большинство контактов получат характеристику» (неявно во всех прежних планах досье) | Большинство получит «данных мало»: CI≥60 требует ≈4–16 разрешённых исходов при идеальных ярлыках; реальных разрешённых исходов у типичного контакта — единицы | C-11, `00_ci_demo.py` | Главный продуктовый эффект — честность, не охват; UI спроектирован вокруг состояния «мало данных» + путь ✓/✗ |
| S-3 | Атрибуция говорящего у фактов есть (промпт/граф «роли») | `who='UNKNOWN'` захардкожен для ВСЕХ structured_facts; поле `fact_type` не пишется | `repository.py:406-415` (D2) | Любая «уклончивость/противоречие» в графе — смесь двух говорящих; сигналы по «кто сказал» возможны только из `promise_outcomes`/`deep_facts` (там who есть) |
| S-4 | Verbatim-цитаты проверяются (`graph.md`: «Quotes MUST be verbatim», validator с ratio 0.72) | В боевом пути (`orchestrator.py:994`, `enricher.py:276`, `uow.py:8`) транскрипт не передаётся → проверка пропускается; только replay | D3 | R-03: включить в боевом пути; до replay граф содержит непроверенные цитаты |
| S-5 | Два BS-канала согласованы (LLM `bs_score` и граф `bs_index`) | Независимы, разные шкалы; `advice` кормится LLM-самооценкой («пиздёж») | D5 | Оба выводятся из production-решений; `advice` переводится на CR |
| S-6 | Буква A–F полезна «ради привычки» (моё же C-18 в round 1) | Шаблонное решение: red/hci/advocate единодушно; цифра info = среднее LLM-confidence (самооценка) | R2 refutation C-17/C-18 | Удалить admiralty целиком; карточка `надёжность:` строкой |
| S-7 | Фиксированные band — естественная дискретизация | Band <0.4 шириной 0.4 давал негативный ярлык после ~2 провалов; равные band 0.2 — boundary-артефакт (истинная 0.2 → CI≤56 навсегда) | `00_ci_demo.py` round 2 | CI = масса posterior в окне ±0.1 вокруг медианы; негативные фразы — n_eff≥4 |
| S-8 | Правая цензура — деталь | Survivor bias от `unknown`/молчания — «fatal» у обоих судей панели дизайнов; ни один дизайн не учёл | judge-1/judge-2 | C-19: E-2 обязателен; покрытие < 0.3 → cap CI 59; `cr-v1c` при MNAR |
| S-9 | 40 adjudicated элементов хватит для калибровки числа (моё round-1 предположение) | Статистик: для решения о калибровке нужно ≥150–200 исходов | R2 C-10 | Двухстадийная калибровка: число включается только когда ✓/✗-петля накопит ≥150 |
| S-10 | `psychology_profiler` паттерны — «структурные», не ярлыки (dashboard.md «паттерны для внимания») | Trait-имена (`blame_shifter`, `vague_communicator`…) на мёртвых входах = ярлыки без данных; `reliable` при `broken≡0` ставится всем с ≥3 обещаниями | D8, C-17 R2 | Удалить 4 паттерна; `promise_breaker/reliable` → CR-фраза |
| S-11 | Synth-корпус покрывает граф | Synth не генерирует `structured_facts`/events/promise_outcomes | data-surface §10 | R-05: расширить synth генератором обещаний+исходов с ground truth, иначе CI не тестируем офлайн |
| S-12 | `contradiction_dens` — живой член формулы | Единственный живой член; `bs_evidence`-«противоречия» от enricher не имеют `entity_id` и не считаются | D1, §4 data-surface | `bs_index` сегодня = 20·доля звонков с LLM-«противоречием» — это и есть всё, что видел владелец под именем «BS» |
| S-13 | Маркеры уверенности — «поведенческий прокси, не детектор лжи» (моё round-2 C-09) | Рецензенты плана единогласно: речевой паттерн → черта, как его ни назови; мощность ≈0.45 | hostile review R-25 | Снято; правило: сигнал из *речи* контакта допустим только как вход предсказания исхода с holdout, никогда как строка UI |
| S-14 | Фразы о поведении («держит слово») ≠ ярлык личности (C-17 round 2) | Deception/ethics: «чаще не выполняет» семантически = «ненадёжен»; граница — время глагола и оговорка «из известных» | hostile review OBJ-4/OBJ-6 | Фразы описательные, прошедшее время; predictive только после E-4 |
