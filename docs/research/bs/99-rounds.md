# 99 — Журнал раундов: метод, выбор алгоритма, расход, критерий остановки

## Round 1 (2026-08-22) — реконструкция + литература
- **Метод:** 3 Explore-агента (sonnet, codegraph) по трём срезам кода → личная перепроверка каждого
  числа grep/sed (найдены расхождения: агент-1 приписал `count_facts_by_type` группировку по
  `fact_type` — на деле по `event_type`, что и делает 4 члена формулы мёртвыми; агент-3 не нашёл
  половину — дочитано вручную). Workflow «lit sweep»: 14 направлений × 1 агент с веб-поиском →
  156 записей → дедуп 151 (142 verified) → 9 adversarial-верификаций опорных утверждений (V-1…V-9).
- **Почему так:** единственная эмпирика на этой машине — код; литература — ширина, не глубина.
- **Результат:** `40-data-surface.md`, `60-existing-bs-verdict.md`, `10-literature.md`.
  Новые факты: D1–D11 (мёртвая формула, who=UNKNOWN, verbatim только в replay, два BS).
- **Расход:** ~3.6M токенов субагентов (lit sweep 3.3M, explore 0.24M). Commit `b224cea`.

## Round 2 (2026-08-22) — first principles, ledger, опровержение, judge panel
- **Метод:** `20-first-principles.md` и `claims-ledger.md` (C-01…C-18) написаны лично от механизма;
  затем два параллельных workflow: (a) refutation — 18 claims × 3–4 дисциплины = 57 рецензий (effort
  high, каждая читает ledger/first-principles/data-surface; 1 агент не вернул структуру);
  (b) design panel — 3 независимых дизайна (stat-first / owner-first / arch-minimal) + 2 судьи
  (третий не вернул структуру). `00_ci_demo.py` — численная проверка C-11.
- **Смена алгоритма по ходу:** (1) фиксированные band заменены скользящим окном ±0.1 после того, как
  демо показало boundary-артефакт и негативный ярлык после 2 провалов (S-7); (2) калибровка разделена
  на две стадии после вето статистика на «40 ярлыков» (S-9); (3) admiralty удаляется целиком (S-6).
- **Статусы:** изменены C-01 (разделён), C-04 (разделён), C-05 (кандидат до E-1/E-2/E-4), C-09
  (переименован, эксперимент), C-10 (двухстадийность), C-17 (усилен), C-18 → retracted; добавлены
  C-19 (цензура), C-20 (порядок гейтов). Новых утверждений: 2; смен статуса: 8.
- **Design panel:** победитель по обоим судьям — Beta-binomial (stat-first) логика + UI «Surface»
  (owner-значимые строки, data-quality строка, тиры evidence); отклонено: счётчики на карточке
  (домен-правило), 30-дневный тренд (слишком редкие исходы) → «недавнее vs общее» только при ≥3
  исходах за 6 мес.
- **Расход:** ~10.1M токенов субагентов (refutation 9.2M, panel 0.95M).

## Round 3 (2026-08-22) — синтез + план (checkpoint, лимит сессии)
- **Сделано:** `70-synthesis.md` (D-1…D-14, трассировка, графты/отказы панели, риски→эксперименты), `90-execution-plan.md` (R-01…R-30, §0–§15), `box-package/scripts/00_ci_demo.py`.
- **НЕ сделано (следующая сессия):** враждебная рецензия плана (workflow: stat/deception/arch/ethics/red → правки или открытые риски), box-package документы (`README.md`, `hypotheses.md`, `protocol.md`, `decision-rules.md`, `adjudication-request.md`) и скрипты 01–08 с `--synth` smoke, проходы доведения (first-principles/фальсифицируемость/анти-шаблон/противоречия между файлами/ясность), `verification-before-completion`, `decisions.md`.
- **Hostile review плана (workflow):** 5 позиций (red-team агент не вернул структуру; 4 рецензии:
  stat=reject, deception/arch/ethics=major revision) → 21 возражение → 21 независимая верификация →
  19 стоят. Принято правкой: S1 (окно у краёв), S2/S5/S6/S8 (мощность/микро-кластеры — записаны до
  данных, пороги сдвинуты в консервативную сторону), S3 (RG-сетка и гард), S4 (чувствительность MAR),
  S7 (утечка holdout), OBJ-2 decept (циркулярность — вопрос «это было обещание?»), OBJ-4/6 decept
  (фразы → описательные прошедшего времени), arch (R-04 sync, R-06/R-07 split), ethics OBJ-2/3/5/7/8
  (заголовок над фактами, re-judge 20%, метод в строке, median_raw, harm-gate w_role). Удалено:
  R-25/R-26 + C-09. Открытые риски: R-10 (точность verdicts), R-11 (фраза читается как черта).
- **Box-package:** README/hypotheses/protocol/decision-rules/adjudication-request + скрипты 00–08
  (06 удалён), все с `--synth` smoke (synth-оракул E-1 восстанавливает q=0.8 → 0.82).
- **Критерий остановки:** план прошёл рецензию с правками; новых claims: 0, retracted: 1 (C-09).
  Формально «два раунда без изменений» не достигнуты — следующий раунд имеет смысл только после
  данных бокса (E-0a/E-1); без них новые утверждения были бы спекуляцией (обоснование остановки).

## Критерий остановки (из промпта §6)
Два раунда подряд без нового утверждения/смены статуса И critic пуст И план прошёл рецензию — ИЛИ
бюджет. Состояние после round 2: не достигнут (8 смен статуса).
