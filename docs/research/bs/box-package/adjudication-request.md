# Запрос на adjudication владельцу (шаблон; заполняется `scripts/02_promise_coverage.py --adjudication-request`)

Сергей Станиславович, ниже ≤40 строк. По каждой — три ответа (впишите в таблицу или в
`adjudication-answers.csv`: `promise_key,is_promise,outcome,role_swapped`):

1. **Было ли это обещание собеседника вам?** — да / нет (нет = система выдумала или это ваше обещание).
2. **Исход:** исполнено / не исполнено / не знаю.
3. **Роли перепутаны?** — да, если цитата «обещания» на самом деле ваша реплика.

Звонок можно прослушать в дашборде: Calls → `call_id` → строка транскрипта (плеер M2).
Ничего другого системой здесь не показано намеренно — ни её оценки, ни источника, чтобы ответ был
независимым.

| # | promise_key | дата | call_id | обещание (цитата) | что слышно потом (цитата, если есть) | 1 | 2 | 3 |
|---|---|---|---|---|---|---|---|---|
| (генерируется скриптом 02 с `--seed 0 --n 30 --overdue 10`) | | | | | | | | |
