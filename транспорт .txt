Транспортный контракт» вписан в OzaluplivanieFable.md. Состав:

8.1 Канон — таблица истины: адрес, туннель WireGuard 10.8.0.0/24 (ПК=10.8.0.1, телефон=10.8.0.2), пути обеих шар, канон имени карточки (только цифры, 8→7), права аккаунта sync, «наружу — только UDP 51820».
8.2 Почему WireGuard — ключевое решение: у ПК публичный IP, а SMB на публичном адресе с записями разговоров — исключено. Наружу торчит один молчащий UDP-порт; телефон синкает из любой точки мира; split-tunnel — остальной трафик телефона через ПК не ходит.
8.3 Windows — 7 шагов с командами: аккаунт sync без интерактивного входа, icacls (с SID-нюансом локализованной Windows — твои грабли из установщика SRP учтены), скрытые шары calls-in$/cards$, конфиг WireGuard, firewall (SMB только из 10.8.0.0/24, проверка снаружи что 445/3389 закрыты), сон выключен, watcher+бот в Планировщике, llama-server НЕ автостартует.
8.4 Телефон — WireGuard app (Always-on, туннелируется только FolderSync), две пары FolderSync (копирование с temp-файлами / зеркало), макрос MacroDroid по шагам с канонизацией номера.
8.5 Чек-лист развёртывания — 10 проверяемых шагов от handshake до «ребут обеих сторон, всё поднялось само», включая обязательную проверку, что запись в cards$ запрещена и порты снаружи закрыты.
8.6 SLO — честные 10–40 мин на цикл «поговорил → карточка обновилась» и единственный невидимый отказ (телефон молча не синкает) с его ловушкой.
Кодовых обязательств из контракта ровно два, оба вшиты в существующие задачи без перенумерации: канон имени карточки → поправка §4.3 п.6 (исполняется в A6 + rebuild-cards), doctor-чек input-silence (72ч тишины входа → WARN) → в спеку F6.



/goal resume

предусмотрена ли при выполнеииплана OzaluplivanieFable2.md исполнение Задачи — Deep-extract: map-reduce по ПОЛНОМУ транскрипту длинных звонков *(T2; LLM-окно), т.е. снять слепоту «head+tail 3000 символов» ? это важнейший пункт