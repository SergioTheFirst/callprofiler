НАЧАЛО:
  → new-session.bat        (одно нажатие)
  → Ctrl+V в Claude        (одно нажатие)
  → Claude сам читает и докладывает

В ПРОЦЕССЕ:
  → Claude сам пишет [Memory updated] после каждого блока
  → Ничего делать не нужно

КОНЕЦ:
  → save-session.bat       (одно нажатие + одна фраза что сделано)

ЛИМИТ КОНЧИЛСЯ:
  → emergency-save.bat     (одно нажатие + одна фраза)
  → следующий раз — опять new-session.bat как обычно