# GigaAM v3 RNN-T ASR stack — Stage-1 (audio → текст).
#
# ⚠ GPU ТРЕБУЕТ Python 3.12 (или 3.11/3.10).
#   PyTorch НЕ собирает CUDA-колёса для Python 3.14 → на 3.14 доступна только
#   CPU-сборка (torch 2.12+cpu), GPU простаивает, GigaAM в 20-50× медленнее.
#   На прогоне 2026-06-03 на боксе стоял Python 3.14 → CPU-only (см. rez.txt).
#
# Установка для GPU (Windows):
#   1) поставить Python 3.12, создать окружение на нём;
#   2) torch + torchaudio под CUDA 12.4 (версии обязаны совпадать):
#        pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#   3) остальное:
#        pip install -r requirements-gigaam.txt
#   4) ffmpeg (НЕ pip): https://www.gyan.dev/ffmpeg/builds/ → bin\ в PATH (ffmpeg -version)
#
# Проверка GPU: python -c "import torch; print(torch.cuda.is_available())"  → True

# transformers <5: GigaAM v3 написан под 4.x; 5.x ломает загрузку
# (all_tied_weights_keys / meta-device) и требует ручных патчей. Держим 4.x.
transformers>=4.40,<5
hydra-core>=1.3
omegaconf>=2.3
sentencepiece>=0.2
numpy
pyyaml
requests

# Dashboard (realtime UI) — для `python -m callprofiler dashboard`:
fastapi
uvicorn
jinja2

# torch==2.6.0 + torchaudio==2.6.0 — ставить отдельно с cu124 index (шаг 2 выше)

# ── ROLES / диаризация (определение [me]/[s2]) ──────────────────────────────
# НЕ ставить через `pip install -r` этого файла: pyannote тянет зависимости,
# которые ЗАТИРАЮТ cu124-torch CPU-сборкой (GPU отвалится). Поэтому ставится
# ОТДЕЛЬНО через install-roles.bat (он переустанавливает cu124-torch после).
#
# БЕЗ этого стека диаризация gracefully падает в orchestrator._diarize_turns и
# ВСЕ роли остаются UNKNOWN (транскрипт не теряется, но кто говорит — неизвестно).
# Что нужно для ролей (всё это делает install-roles.bat):
#   1) pip install pyannote.audio soundfile librosa  (затем fix-torch.bat!)
#   2) HF_TOKEN в окружении: setx HF_TOKEN "hf_..."  (новый терминал)
#   3) принять 3 gated-модели на hf.co: pyannote/segmentation-3.0,
#      pyannote/speaker-diarization-3.1, pyannote/embedding
#   4) эталон голоса владельца --ref-audio <owner.wav> (дефолт C:\pro\mbot\ref\manager.wav)
# pyannote 3.3.2: в коде use_auth_token= (НЕ token=). См. .claude/rules/decisions.md.
#   pyannote.audio==3.3.2
#   librosa
#   soundfile
