# fixager.md — Исправление препятствий определения возраста (marker + age_style)

> **Для кого:** Claude Code (Sonnet), исполняющий агент. Контекст: `age.md` (план,
> реализован целиком), `vozrast.md` (методология), карта `.claude/rules/insight.md`.
> **Что это:** аудит РЕАЛИЗОВАННОГО кода нашёл конкретные баги/препятствия, которые
> роняют точность возраста. Здесь — их исправление. Максимальный реюз существующего
> кода: ни одного нового модуля, только правки данных/функций + 1 кнопка.
> **Правило исполнения:** каждая задача самодостаточна, не додумывать сверх написанного.
> Тир — в шапке фазы (Model Routing, `CLAUDE.md`). После каждой фазы:
> `$env:PYTHONPATH="C:\pro\callprofiler\src"; python -m pytest tests/insight tests/test_dashboard_age_style.py -q` зелёный.

---

## Найденные препятствия (сводка аудита, по убыванию вреда)

| # | Препятствие | Где | Эффект |
|---|---|---|---|
| P1 | Ложные срабатывания лексиконов через `startswith`: «база»→базар/базы, «аккурат»→аккуратно, «диал»→диалог, «краш»→крашеный, «чил»→чили, «ласковый»→обычное прилагательное, «талон»→талончик к врачу (совр.), «тату»→тату(ировка), «сериал»/«дач»/«школ»→у всех возрастов | `age_style/lexicons/*.txt` + `_stem_hit` в `features/lexical_age.py` | Систематическое смещение: «аккуратно» старит ЛЮБОГО, «диалог» даёт год рождения 1980-1990 с весом 0.90, «сериал» тянет в G6 с весом 0.95. Главный убийца точности |
| P2 | `support_n` = ВСЕ токены контакта, а не число попаданий: `life_stage_profile` возвращает `support_n=len(norm)`, `realia` получает `support_n=extra["total_tokens"]` (`estimate_style.py:187`) | `lexical_age.py:57`, `estimate_style.py:187` | 1 ложное попадание среди 5000 токенов → полный вес голоса (n0=20/10 достигается мгновенно). Усиливает P1 до максимума |
| P3 | `stale_only=True` считает z-score по ПОДВЫБОРКЕ stale-контактов, не по популяции: `target_ids` фильтруется ДО `assemble_matrix`/`standardize` | `estimate_style.py:123-130,152-157` | Watcher-autofit ежедневно пишет строки с бинами, посчитанными относительно 1-3 контактов → мусорные группы. Активная порча данных |
| P4 | Ложная точность интервала: 6 дискретных центров групп → при доминирующей группе low==high==point → `half=max(1, 0·widen)=1` → интервал ±1 год при conf 25 | `estimate_style.py:210-213` + `accumulate.to_birth_year` | «~48 лет (47–49)» при доверии 25/100 — вводит в заблуждение |
| P5 | Маркеры-этапы без контекст-гардов: «выйду на пенсию» (будущее, говорит 40-летний) → 60-80; «у дочки скоро ЕГЭ» — «дочки» НЕ ловится `_RE_THIRD_PERSON` (`доч(?:ь\|ке\|ка)` без «дочки/дочек») → родитель классифицируется 16-18 | `age_markers.py:63,71,82-85` | Класс-2 сигналы с conf 60-70 перекрывают relation-якоря и портят агрегат |
| P6 | Конфликт низшего класса РОНЯЕТ уверенность высшего до min+10: один ложный ЕГЭ (P5) при твёрдом «мне 45» (conf 90) → итог conf 70→80... а при ложном stage-conf 60 → 70. Прямой маркер обесценивается мусором | `age_estimate.py:_aggregate:112-115` | Хорошие оценки выглядят сомнительными |
| P7 | Кнопка «Определить возраст ↻» рендерится ТОЛЬКО при существующей строке `age_style` (`if (d.age_style)`) и пересчитывает ТОЛЬКО стиль — маркер-пасс из UI недоступен вообще | `app.js:892,936`, `tools.py:_age_recompute_sync` | Контакт без строки стиля не имеет кнопки; «точнее» из UI не сделать |
| P8 | `owner_birth_year=0` на боксе (base.yaml не задан) → relation-якоря молча выключены; ноль диагностики | `config.py:98`, ops | Целый класс сигналов мёртв без единого warning |
| P9 | Диаризация хрупкая → speaker='UNKNOWN' → `skipped_no_data`, секция стиля пустая без объяснения | `estimate_style.py:141` (by design), UI | Юзер видит «нет данных», не зная причину |

---

## Фаза 1 — Лексиконы: чистка + точный матч  *(T1: Opus /fast; данные + 1 функция)*

**Цель:** убить P1. Правки данных + режим точного матча для коротких/омонимичных стемов.

| # | Действие | Детали |
|---|---|---|
| 1.1 | Формат лексикона: флаг `=` | В `age_style/lexicons.py::load_lexicon`: стем, начинающийся с `=`, означает ТОЧНОЕ совпадение токена (после `normalize_lemma`), не `startswith`. Убрать `=` из `cols[0]`, добавить в tuple последним элементом маркер `exact:bool` — НЕТ, проще: вернуть rows как раньше, но в `lexical_age.py::_stem_hit` — если стем в загруженном виде был с `=`, сравнивать `token == stem`. Реализация: `load_lexicon` возвращает стемы уже без `=`, а рядом кладёт префикс в отдельное поле — минимальный диff: `cols[0]` оставить с `=`, а `_stem_hit`: `if s.startswith('='): return token == s[1:]; return token.startswith(s)`. `life_stage_profile`/`realia_birth_year` используют тот же `_stem_hit` (сейчас там inline `t.startswith(stem)` — заменить на вызов `_stem_hit`). |
| 1.2 | `slang.txt` | Удалить `база` (базар/базы/базовый), `чил` (чили), `краш` (крашеный) ЛИБО перевести в точную форму: `=база`, `=чил`, `=краш`, `=кринж`, `=рофл`, `=вайб`, `=зашквар`. Расширить (точные формы): `=имба`, `=имбов`, `=лол`, `=пруф`, `=пруфы`, `=агриться`, `=агрится`, `=токсик`, `=душнил`, `=душн`, `=скинь`, `=го` — стоп, `го` слишком коротко/омонимично, НЕ добавлять. Добавить: `=криндж`, `=флексить`, `=чекни`, `=чекнуть`, `=изи`, `=жиза`, `=треш`, `=трешак`, `=хайп`, `=хайпов`, `=зумер`. Всё односложно-омонимичное — только `=`-формы. |
| 1.3 | `archaisms.txt` | Удалить `аккурат` (аккуратно!) — заменить на `=аккурат` (наречие «аккурат в пять»), удалить `талон` (совр. «талончик к врачу»; советский смысл неотличим). Оставить: `давеча`, `=оное`, `эвм`, `сберкнижк`, `партком`, `=блат` (блатной ≠ советизм в этом смысле, точная форма). Добавить: `нынче`, `покамест`, `=отнюдь`, `гривенник`, `профком`, `собес`, `=кабы`, `касса взаимопомощи` — многословные НЕ поддерживаются токенайзером, пропустить. Добавить однословные: `профком`, `собес`, `гривенник`, `нынче`, `покамест`. |
| 1.4 | `realia_by_epoch.txt` | Удалить `диал` (диалог!), `ласковый` (обычное прилагательное), `тату` (современное у всех), `талон` (дубль archaisms + совр.). `денди`→`=денди`. Добавить точные/безопасные: `пейджер` (ок), `=сега`, `дискет	1930	1985`, `видеомагнитофон	1930	1985`, `=аська	1980	1995`, `=вкладыш` — омоним (документы), НЕ добавлять. `тикток`/`твич` ок; `аниме` — смотрят и 35-летние, сместить интервал `1985	2010`. |
| 1.5 | `life_stage.txt` | Удалить `сериал` (все возрасты), `дач` (все возрасты), `съём` (съемка; после ё→е — «съем»). `школ` — оставить, но см. Фазу 2 (гейт ≥2 РАЗНЫХ стемов кластера снимет «родитель о ребёнке» в большинстве случаев); `карьер` оставить. Добавить: `стипенди` уже есть; `зачёт	вуз_сессия`, `курсов	вуз_сессия`, `ординатур	вуз_сессия`, `садик`/`детсад` есть; `родительск	ипотека_декрет` (родительское собрание = родитель школьника, G3-G4!), `алимент	ипотека_декрет`, `внучк	внуки_пенсия` (есть `внуч`), `давлени	внуки_пенсия` — слишком общий (у всех давление на работе), НЕ добавлять. |
| 1.6 | Регресс-тест | `tests/insight/test_age_lexicons_fp.py`: `test_exact_match_stems` («мы были на базаре», «чили острый», «крашеный забор» → slang_density 0); `test_akkuratno_not_archaism` («сделай аккуратно» → archaism_density 0); `test_dialog_not_realia` («у нас был диалог» → realia None); `test_serial_not_life_stage` («смотрю сериал» → cluster None); `test_real_slang_detected` («это кринж и зашквар» → density > 0); `test_real_archaism_detected` («давеча в собесе» → density > 0). |

**DoD:** тесты 1.6 зелёные; старые `tests/insight/test_age_features.py` зелёные (при
падении из-за удалённых стемов — обновить фикстуры теста на оставшиеся стемы, это
ожидаемо и допустимо).

---

## Фаза 2 — support_n = попадания, не корпус  *(T2: Opus high; контракт признака)*

**Цель:** убить P2 — один случайный хит не должен голосовать полным весом.

| # | Действие | Детали |
|---|---|---|
| 2.1 | `life_stage_profile` | `lexical_age.py`: возвращать `support_n = hits` (число попаданий доминирующего кластера), НЕ `len(norm)`. Плюс гейт: доминирующий кластер засчитывается только если в нём ≥2 попаданий И ≥2 РАЗНЫХ стемов (родитель, один раз сказавший «школа», отсекается; настоящий школьник говорит «школа», «урок», «ЕГЭ» многократно). Меньше → `{"cluster": None, ...}`. Реализация: считать `Counter` по (cluster, stem), собрать per-cluster hits и distinct-stems. |
| 2.2 | `realia_birth_year` | Аналогично: вернуть также число попаданий — сменить сигнатуру на `-> tuple[tuple[int,int], int] | None` ((year_range), hits) ЛИБО (меньший blast radius) оставить сигнатуру и добавить гейт внутри: доминирующая эпоха засчитывается только при ≥2 попаданиях; иначе None. Взять второй вариант (сигнатура не меняется, вызывающие не трогаются). |
| 2.3 | `estimate_style.py:187` | `features["realia"]["support_n"]`: заменить `extra["total_tokens"]` на фиксированное разумное `10` (= n0, полный вес — гейт ≥2 хитов уже отработал в 2.2) — ponytail: честный support потребовал бы смены сигнатуры 2.2; при калибровке §15 перейти на вариант с hits. Для `life_stage` ничего менять не надо — support_n теперь hits из 2.1 (проверить, что `SUPPORT_N0["life_stage"]=20` теперь слишком высок: 20 попаданий редкость → снизить до `5`). |
| 2.4 | Тесты | В `tests/insight/test_age_features.py` добавить: `test_life_stage_single_hit_rejected` (1×«школа» среди 500 нейтральных токенов → cluster None); `test_life_stage_two_distinct_stems_accepted` («школа… урок… ЕГЭ» → cluster «школа_егэ», support_n = hits, не len); `test_realia_single_hit_rejected` (1×«пейджер» → None; 2× → интервал). |

**DoD:** тесты зелёные; `pytest tests/insight -q` без регрессий (существующие
тесты scorer/estimate, где синт-профили дают МНОГО хитов, должны пройти как есть).

---

## Фаза 3 — z-популяция при stale_only  *(T2: Opus high; watcher пишет мусор — data-баг)*

**Цель:** убить P3. z всегда по полной популяции; stale-фильтр — только на записи.

| # | Действие | Детали |
|---|---|---|
| 3.1 | `estimate_style.run_style_estimate` | Перестроить: собрать `per_contact_matrix` для ВСЕХ контактов с данными (независимо от stale), посчитать `assemble_matrix`+`standardize` по всем; stale-фильтр применить в цикле записи (`for i, cid in enumerate(cids): if stale_only and cid in fresh_set: stats["skipped_fresh"] += 1; continue`). Стоимость: фичи пересчитываются для всех даже в инкременте — приемлемо (numpy/regex, без GPU; кнопка в дашборде и так гоняет полную популяцию). `# ponytail:` пометить: кэш сырых фич per contact — апгрейд при тормозах на 16k. |
| 3.2 | Мини-популяция | Там же: если контактов с данными < 5 — z-бины вырождены (sd≈0 → всё «среднее»). Не блокировать (лексикон/маркер-голоса не зависят от z), но добавить warning `"малая популяция для z-нормировки"` в warnings каждой строки при `len(cids) < 5`. |
| 3.3 | Тест | `tests/insight/test_age_style_estimate.py` добавить: `test_stale_only_z_uses_full_population` — популяция 10 контактов, у одного новый звонок; прогон `stale_only=True`; его group_json ДОЛЖЕН совпасть с прогоном `stale_only=False` (z одинаковы); `skipped_fresh == 9`. |

**DoD:** тест зелёный; существующий `test_idempotent` зелёный.

---

## Фаза 4 — Честная ширина интервала  *(T1: Opus /fast; одна формула)*

**Цель:** убить P4 — не показывать ±1 год при низком доверии.

| # | Действие | Детали |
|---|---|---|
| 4.1 | `estimate_style._score_and_save` | Пол ширины: полуширина не меньше половины span доминирующей группы. `dom = max(p_group, key=p_group.get)`; `span = GROUP_RANGES[dom][1] - GROUP_RANGES[dom][0]` (для G6 использовать 20, не 200-60: `min(span, 20)`); `half = max(span / 2.0, (birth_high - birth_low) / 2.0) * widen` (заменить текущее `max(1, ...)`). Импорт `GROUP_RANGES` из `.tables`. |
| 4.2 | Тест | `test_age_style_estimate.py`: `test_interval_not_false_narrow` — доминирующая G4 (span 9) при conf<50 → `birth_high - birth_low >= 9`. Обновить ассерты существующих тестов, если проверяли узкий интервал. |

**DoD:** тесты зелёные.

---

## Фаза 5 — Маркер-гарды + мягкий конфликт  *(T2: Opus high; money-path `_aggregate` — контролируемая правка + регресс)*

**Цель:** убить P5/P6. Точечные гарды по образцу существующих (`_RE_GRANDKIDS_NOT`).

| # | Действие | Детали |
|---|---|---|
| 5.1 | `age_markers.py`: pension-future гард | По образцу `_RE_GRANDKIDS_NOT`: `_RE_PENSION_FUTURE = re.compile(r"выйд[уе]\w*|через\s+\S+\s*(?:лет|год)|до\s+пенси|накоп|будущ|доживу|когда\s+вы[йи]д", re.I)`. В цикле `_STAGES` для `pension`: пропуск, если `_RE_PENSION_FUTURE.search(text[max(0,m.start()-40):m.end()+20])`. |
| 5.2 | Расширить `_RE_THIRD_PERSON` | Добавить формы: `доч(?:ь|к\w*)` (покрывает дочки/дочкой/дочек), `сын\w*` вместо `сын[уа]?`, `внук\w*|внучк\w*`, `ребпариж` — нет: `реб[её]нк\w*|дет(?:и|ей|ям|ьми)`, `у\s+(?:дочк|сына|детей|ребенка|реб[её]нка)`. Окно поиска `-18` символов мало для «у дочки скоро» → расширить до `-24`. |
| 5.3 | ЕГЭ-гард | Для `school_exam` в `_STAGES`-цикле доп. проверка (по образцу grandkids): `_RE_EXAM_NOT_SELF = re.compile(r"доч|сын|реб[её]нк|дет[еиь]|учени|клас[cс]е\s+у|репетитор", re.I)` по окну ±30 — матч → пропуск. |
| 5.4 | `age_estimate._aggregate`: мягкий конфликт | Заменить правило «конфликт низшего класса → `conf = min(contributing)+10`» на: конфликт ВНУТРИ класса — как было (конверт + min+10); конфликт НИЗШЕГО класса с высшим — интервал не трогается (уже так), `conf -= 10` за каждый конфликтующий низший сигнал (floor 1), НЕ обвал до min. Прямое «мне 45» (90) больше не обесценивается ложным этапным маркером. Это правка money-path: НЕ трогать ничего кроме этой ветки (`for s in sorted(rest, ...)` / финальный `if conflict:`). |
| 5.5 | Тесты | `tests/insight/test_age_markers_guards.py`: `test_pension_future_not_marker` («выйду на пенсию через пять лет», «накоплю до пенсии» → 0 pension-сигналов); `test_pension_real_detected` («я на пенсии уже три года» → сигнал); `test_ege_of_child_rejected` («у дочки скоро ЕГЭ», «сын сдаёт ЕГЭ» → 0); `test_ege_self_detected` («завтра сдаю ЕГЭ» → сигнал); `test_aggregate_lowclass_conflict_soft` (direct 90 + конфликтующий stage 60 → conf == 80, интервал от direct). Прогнать ВЕСЬ `tests/insight` — `_aggregate` покрыт существующими тестами, они обязаны остаться зелёными (кроме теста, прямо проверявшего старое min+10 правило для низшего класса — его обновить с комментарием). |

**DoD:** новые тесты зелёные, регрессы зелёные. Гейт: правка `_aggregate` = контракт
агрегатора — самопроверка диффа, ничего кроме conflict-ветки не изменено.

---

## Фаза 6 — Кнопка «Точный пересчёт возраста» (маркеры + стиль)  *(T1 UI + T2 endpoint)*

**Цель:** P7/P8 — из досье запускается ПОЛНЫЙ детерминированный пересчёт: маркер-пасс
(этого контакта) + стиль (вся популяция). Без LLM (доктрина дашборда, bugs 2026-06-11).

| # | Действие | Детали |
|---|---|---|
| 6.1 | `dashboard/tools.py::_age_recompute_sync` | Перед `run_style_estimate` добавить маркер-пасс: `from callprofiler.insight.age_estimate import run_age_estimate` → `mstats = run_age_estimate(conn, self.user_id, contact_id=contact_id, owner_birth_year=getattr(self.config, "owner_birth_year", 0) or 0)` (только этот контакт — дёшево; `use_llm` НЕ передавать). Стиль после него читает свежий маркер через `_get_marker`. В ответ добавить `"marker_stats": mstats` и секцию `"age"` досье (не только age_style). Тот же conn (`repo._get_conn()`). |
| 6.2 | `app.js`: кнопка всегда | Вынести кнопку из `if (d.age_style)`: рендерить блок «Возраст (стиль)» ВСЕГДА в досье; при `!d.age_style` — текст «не рассчитано» + та же кнопка (первый расчёт из UI). Переименовать кнопку: `Точный пересчёт возраста ↻` (title: «маркеры + стилометрия, без LLM»). Обработчик уже перезагружает досье — не трогать. |
| 6.3 | Строка «Явные маркеры» | В блоке стиля (app.js:930) при наличии `d.age` показывать не просто «есть», а `метод + интервал`: `'есть: ' + d.age.method + (d.age.age_point != null ? ', ~' + d.age.age_point + ' лет' : '')`. |
| 6.4 | Диагностика P8/P9 | `tools._age_recompute_sync`: если `getattr(self.config, "owner_birth_year", 0) == 0` — добавить в ответ `"hint": "owner_birth_year не задан в base.yaml — реляционные якоря выключены"`. app.js: показать hint под кнопкой (муted). В `estimate_style`: `skipped_no_data` уже в stats; в `get_person_dossier` (db_reader) при отсутствии строки стиля И отсутствии OTHER-реплик — ничего не менять (не лезть в лишний SQL), достаточно текста «не рассчитано» из 6.2. |
| 6.5 | Тесты | `tests/test_dashboard_age_style.py` добавить: `test_age_recompute_runs_marker_pass` (сеем транскрипт с «мне сорок пять лет» от OTHER → POST age-recompute → в `contact_age_estimates` появилась строка method='marker' И в ответе есть marker_stats); `test_age_recompute_hint_when_no_owner_birth_year`. Существующий `test_age_recompute_endpoint_writes_and_returns` — зелёный. |

**DoD:** кнопка видна у контакта без строки стиля; клик создаёт и маркер-, и
стиль-оценку; hint при незаданном `owner_birth_year`; LLM не вызывается нигде.

---

## Ops (бокс, не код — записать в CONTINUITY Next)

1. `base.yaml`: задать `owner_birth_year` (P8) — иначе реляционные якоря мертвы.
2. После деплоя Фаз 1-5: полный `age-style --user me` + `age-estimate --user me`
   (таблица перезапишется новыми правилами; `TABLE_VERSION`/`RULES_VERSION` бампнуть:
   `age-style-v2` / `age-rules-v2` в `tables.py` — Фаза 1 меняет данные лексиконов,
   Фазы 2-4 меняют семантику; бамп обязателен, паттерн PROMPT_VERSION).
3. Спот-чек 10 знакомых контактов (план `age.md` DoD Ф5) — теперь без P1-мусора.

## Guardrails

- НЕ трогать: схему `contact_age_estimates`/`contact_age_style`, LLM-пасс
  (`age_v001.txt`, `_validate_llm`), дашборд-доктрину (никакого LLM из UI),
  `_get_marker` (исключение 'llm' — намеренное).
- `_aggregate` — только conflict-ветка (Фаза 5.4), больше ни строчки.
- Каждый SQL — `WHERE user_id = ?` (новых SQL здесь почти нет — только реюз).
- Бамп `TABLE_VERSION`/`RULES_VERSION` — один раз, в конце (Ops п.2).

## Порядок

```
Ф1 (лексиконы) → Ф2 (support_n) → Ф3 (z-популяция) → Ф4 (интервал) → Ф5 (маркер-гарды) → Ф6 (кнопка)
```
Ф1-Ф4 независимы от Ф5-Ф6, но порядок сохранять (тесты Ф2+ опираются на чищеные лексиконы).

**Готовность всего:** `pytest tests/ -q` зелёный; кнопка «Точный пересчёт возраста ↻»
работает у контакта без предыдущей оценки; память (CONTINUITY/CHANGELOG/insight.md) →
commit → push (`CLAUDE.md`).
